// Placeholder — Firebase is optional. App works without it.
// To enable Crashlytics: run `flutterfire configure` to generate real config.
// Until then, Firebase init silently fails (app works fine).
class DefaultFirebaseOptions {
  static const firebaseOptions = null;
}